<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@android:color/white">

    <!-- Dark header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#0D1B2A"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="IMAGES"
            android:textColor="#9CA3AF"
            android:textSize="11sp"
            android:letterSpacing="0.15"
            android:textAllCaps="true" />

        <TextView
            android:id="@+id/tvImageDeleteTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginTop="4dp" />

    </LinearLayout>

    <!-- Body -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="17sp"
            android:textStyle="bold"
            android:textColor="#0D1B2A"
            android:layout_marginBottom="14dp" />

        <!-- Delete Image Only -->
        <Button
            android:id="@+id/btnDeleteImageOnly"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="10dp"
            android:text="Delete Image Only"
            android:textColor="#D32F2F"
            android:textSize="14sp"
            android:backgroundTint="#FFF5F5"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            style="?attr/materialButtonOutlinedStyle"
            app:strokeColor="#FFCDD2"
            app:strokeWidth="1dp"
            app:cornerRadius="8dp" />

        <!-- Delete Image & Event -->
        <Button
            android:id="@+id/btnDeleteImageAndEvent"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="8dp"
            android:text="Delete Image &amp; Associated Event"
            android:textColor="#D32F2F"
            android:textSize="14sp"
            android:backgroundTint="#FFF5F5"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            style="?attr/materialButtonOutlinedStyle"
            app:strokeColor="#FFCDD2"
            app:strokeWidth="1dp"
            app:cornerRadius="8dp" />

        <!-- Warning -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="⚠ Deleting the event will also remove all waitlist entries and related data."
            android:textSize="12sp"
            android:textColor="#9CA3AF"
            android:textStyle="italic"
            android:layout_marginBottom="16dp" />

        <!-- Cancel -->
        <Button
            android:id="@+id/btnImageDeleteCancel"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="CANCEL"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:backgroundTint="#0D1B2A"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:cornerRadius="8dp" />

    </LinearLayout>

</LinearLayout>